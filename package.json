{
  "name": "synced-cron",
  "version": "0.0.1",
  "description": "Synchronized cron jobs for Meteor 3.x across multiple instances",
  "license": "MIT",
  "author": {
    "name": "Anonyfox",
    "email": "max@anonyfox.com",
    "url": "https://anonyfox.com"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/Anonyfox/meteor-synced-cron"
  },
  "homepage": "https://github.com/Anonyfox/meteor-synced-cron#readme",
  "bugs": {
    "url": "https://github.com/Anonyfox/meteor-synced-cron/issues"
  },
  "keywords": [
    "meteor",
    "cron",
    "scheduler",
    "jobs",
    "synchronized",
    "mongodb"
  ],
  "private": true,
  "scripts": {
    "test": "npm run test:type && npm run test:lint && npm run test:code",
    "test:type": "tsc --noEmit",
    "test:lint": "npx @biomejs/biome lint .",
    "test:code": "TEST_CLIENT=0 meteor test-packages ./ --once --driver-package meteortesting:mocha --raw-logs",
    "format": "npx @biomejs/biome format --write .",
    "check": "npx @biomejs/biome check --write ."
  },
  "devDependencies": {
    "@types/chai": "^5.0.1",
    "@types/meteor": "^2.9.8",
    "@types/mocha": "^10.0.10",
    "chai": "^5.1.2",
    "typescript": "^5.4.5"
  },
  "meteor": {
    "modern": true
  }
}
